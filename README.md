# vagrant-docker-jenkins
##### Launching Jenkins from vagrant using docker provisioning

Before running please make sure that 8080 PORT avaialble in your machine

```
sudo vagrant plugin install vagrant-vbguest
sudo vagrant up
```
